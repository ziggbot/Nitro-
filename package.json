{
  "name": "nitro-io",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "NITRO.IO — a modern arena driving game inspired by Nitro (Psygnosis, 1990) and snake.io",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
